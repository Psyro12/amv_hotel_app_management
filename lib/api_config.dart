// lib/api_config.dart

class ApiConfig {
  // Hosted domain for the live backend
  static const String serverIp = "amvhotel.online"; 

  // Base URL for your API (using HTTPS for production)
  static const String baseUrl = "https://$serverIp/API";
}